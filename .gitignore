# Secrets
.env
.env.*
!.env.example

# Private files
private/
CLAUDE.md

# Python build/tool caches
__pycache__/
*.py[cod]
.venv/
*.egg-info/
build/
dist/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
.ipynb_checkpoints/

# Data, outputs and tracking should be in S3
# Note: A plain data/ would match a folder named data at any depth. That would silently ignore the planned code package src/sts/data/, which holds schema.py and perturb.py. The slash limits the pattern to the repo root.
/data/
/outputs/
/mlruns/
*.parquet
!tests/fixtures/**/*.parquet

# Model weights
*.safetensors
*.bin
*.pt
*.pth
*.gguf
*.ckpt
